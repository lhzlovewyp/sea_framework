# ip
bind 0.0.0.0

# 启用 cluster
cluster-enabled yes

# 指定 cluster config 
cluster-config-file nodes.conf

# 指定 node 无法连接时间
cluster-node-timeout 5000

# 设置主服务连接密码
masterauth pass.123

#设置从服务连接密码
requirepass pass.123